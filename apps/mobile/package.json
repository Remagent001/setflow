{
  "name": "@setflow/mobile",
  "version": "0.1.0",
  "private": true,
  "description": "SetFlow mobile companion app. Skeleton only in Segment 00 - the real Expo (React Native) scaffold lands in Segment 10 per the build plan, so early installs stay fast.",
  "main": "src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  }
}

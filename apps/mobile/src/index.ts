// SetFlow mobile companion - Segment 00 placeholder.
// The real Expo app (screens: login, today's workout, workout detail, active
// player, history, settings) is scaffolded in Segment 10 per the build plan.

export const APP_NAME = "SetFlow";

export function placeholder(): string {
  return `${APP_NAME} mobile skeleton - Expo scaffold arrives in Segment 10.`;
}
